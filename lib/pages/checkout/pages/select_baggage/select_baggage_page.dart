import 'package:flutter/material.dart';

class SelectBaggagePage extends StatelessWidget {
  const SelectBaggagePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
